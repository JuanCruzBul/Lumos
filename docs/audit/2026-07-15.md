# Auditoría de código — 2026-07-15

## Resumen ejecutivo

El código sigue en buen estado general: `tsc --noEmit` y `next lint` pasan limpios, no hay `any`, no hay TODOs/FIXMEs reales, y el árbol de git está limpio. Los 9 ítems de deuda técnica abiertos en la auditoría del 2026-07-05 (TD-009 a TD-017, salvo los resueltos) siguen vigentes sin cambios de código que los afecten. Se agrega un ítem nuevo de severidad baja: artefactos de debugging/testing manual (`screenshot-contact.js`, `test-results/`) commiteados al repo sin que exista una suite de tests real, pese a que `playwright` figura como dependencia.

## Alcance

Se revisaron `app/` (layout, páginas, API route de contacto), todo `components/ui/` (incluyendo `components/ui/demo/` y `control-panel/`), `lib/`, configuración (`package.json`, `tsconfig.json`, `.gitignore`, `CLAUDE.md`) y se corrieron `npx tsc --noEmit` y `npx next lint`.

## Hallazgos

### 1. Ítems de la auditoría anterior confirmados vigentes (sin cambios)

- **Severidad**: Media / Baja (ver desglose abajo)
- **Archivos**: ver cada TD en `docs/implements/tech_debt.md`
- **Descripción**: Se releyó el código de cada ítem abierto en la auditoría 2026-07-05 y no hubo cambios que los resuelvan o modifiquen: TD-009 (animaciones corriendo fuera de viewport en `sistemas-section.tsx` y `nosotros-section.tsx`), TD-010 (hydration mismatch en formularios), TD-011 (`CLAUDE.md` desactualizado — la brecha no se cerró: siguen sin documentarse `components/ui/demo/control-panel/`, `scene-presets.ts`, `types.ts`), TD-012 (`useInView` reimplementado a mano en `integraciones-section.tsx`), TD-013 (data + íconos mezclados con lógica en `sistemas-section.tsx`), TD-014 y TD-015 (`room-scene.tsx` sigue mezclando responsabilidades y sin `dispose()` de geometrías/materiales — confirmado en línea 292 y 427-428), TD-016 (`three@0.161.0` vs `@types/three@0.185.0`, confirmado en `node_modules`), TD-017 (los 6 archivos de `control-panel/` siguen sin `"use client"` en el encabezado).
- **Por qué es deuda técnica**: ver cada ítem individual en `tech_debt.md`.
- **Ítem relacionado**: `docs/implements/tech_debt.md#td-009` a `#td-017`

### 2. Artefactos de testing manual commiteados sin suite de tests real

- **Severidad**: Baja
- **Archivos**: `screenshot-contact.js:16` (path hardcodeado `C:/Users/Juanchi/AppData/Local/Temp/...`), `test-results/.last-run.json`
- **Descripción**: `screenshot-contact.js` es un script ad-hoc de Playwright para sacar una captura de la sección de contacto, con un path de salida absoluto de la máquina de otro desarrollador (Windows, usuario "Juanchi") hardcodeado. `test-results/.last-run.json` es el artefacto de una corrida de Playwright (`status: "failed"`) que quedó commiteado. No existe `playwright.config.ts` ni carpeta `tests/` — `playwright` está como devDependency pero no hay una suite de tests real, solo este script suelto.
- **Por qué es deuda técnica**: El path hardcodeado hace que el script falle para cualquier otro desarrollador que lo corra tal cual; el `test-results/.last-run.json` commiteado es ruido de estado local que no aporta información (reporta un fallo sin detalle) y puede confundir a quien lo vea pensando que hay una suite de CI corriendo. Más de fondo: el proyecto no tiene ningún test automatizado pese a tener la dependencia instalada.
- **Ítem relacionado**: `docs/implements/tech_debt.md#td-018`

## Métricas rápidas

- Ítems de deuda técnica encontrados: 10 (9 confirmados vigentes + 1 nuevo)
- Severidad alta: 0 · media: 5 · baja: 5
- `tsc --noEmit`: limpio · `next lint`: limpio · `git status`: working tree limpio
